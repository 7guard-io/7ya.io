name: Quarantined — completed Meta AI discovery enablement

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  quarantine:
    runs-on: ubuntu-latest
    steps:
      - name: Explain quarantine
        run: |
          echo 'QUARANTINED_WORKFLOW'
          echo 'The historical Meta/Cloudflare enablement path is retained for provenance only. It performs no Cloudflare mutation and no production verification loop.'
