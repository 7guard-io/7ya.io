name: Quarantined — completed AppDeploy DNS cutover

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  quarantine:
    runs-on: ubuntu-latest
    steps:
      - name: Explain quarantine
        run: |
          echo 'QUARANTINED_WORKFLOW'
          echo 'The one-time 7ya.io DNS cutover is complete and the canonical domains are active on AppDeploy. This workflow performs no DNS mutation.'
