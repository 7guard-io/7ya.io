import {useEffect,useMemo,useRef,useState} from 'react';
import {ArrowUpLeft,ChevronDown,ChevronUp,ExternalLink,FolderSearch,Play,Search,ShieldCheck,X} from 'lucide-react';
import {deepMedia,deepMediaCategories,type DeepMediaItem,type DeepMediaCategory} from './deep-media-data';
import MediaEvidenceVisual from './MediaEvidenceVisual';
import './deep-media-library.css';
const lens:Record<DeepMediaCategory,string>={טלוויזיה:'איגור אינו רק נושא הראיון — הוא החוט שמחבר ניסיון אישי, אחריות ציבורית והמשך פעולה.',פודקאסטים:'בשיחה ארוכה איגור בונה סיבתיות: מאיפה הגיע, מה נשבר, ומה אפשר לבנות לאחר מכן.',עיתונות:'כתבה היא תחנת ראיה בתוך מסע ארוך יותר; האדם נשאר קבוע גם כשהכותרת והמו״ל משתנים.',כתיבה:'הכתיבה מאפשרת לאיגור לתרגם חיים, הורות, רשתות וחברה למסגרת שאפשר לחשוב איתה.',ויראלי:'רגע ויראלי הוא לא הזהות כולה. 7YA מחזירה אותו אל האדם, ההקשר והאחריות.',מוזיקה:'Ido Vepretski הוא התדר היצירתי של אותו אדם — מוזיקה, הומור וזיכרון תרבותי.',רשתות:'הפלטפורמות משתנות; איגור, הקול והמקור הקנוני צריכים להישאר ניתנים למציאה.',StartOn:'StartOn היא השליחות שנולדה מהמסע האישי, אך נשארת ישות חברתית נפרדת וברורה.'};
export default function DeepMediaLibrary(){const [category,setCategory]=useState<(typeof deepMediaCategories)[number]>('הכול');const [query,setQuery]=useState('');const [activeId,setActiveId]=useState('fraud-13');const [playing,setPlaying]=useState(false);const [expanded,setExpanded]=useState(false);const stageRef=useRef<HTMLDivElement|null>(null);const filtered=useMemo(()=>deepMedia.filter(item=>(category==='הכול'||item.category===category)&&(!query||[item.title,item.source,item.summary,item.year,item.category].join(' ').toLowerCase().includes(query.toLowerCase()))),[category,query]);const active=filtered.find(item=>item.id===activeId)||filtered[0]||deepMedia[0];const visible=expanded?filtered:filtered.slice(0,12);const sources=new Set(deepMedia.map(item=>item.source)).size;const activeIndex=Math.max(0,deepMedia.findIndex(item=>item.id===active.id));useEffect(()=>setPlaying(false),[active.id]);const chooseCategory=(value:(typeof deepMediaCategories)[number])=>{setCategory(value);setQuery('');setExpanded(false);const first=value==='הכול'?deepMedia[0]:deepMedia.find(item=>item.category===value);if(first)setActiveId(first.id)};const chooseItem=(item:DeepMediaItem)=>{setActiveId(item.id);setPlaying(false);if(window.innerWidth<=850)window.setTimeout(()=>stageRef.current?.scrollIntoView({behavior:'smooth',block:'start'}),40)};return <section className='deep-media-library' id='media' aria-labelledby='deep-media-title'><header className='deep-media-head'><div><span><FolderSearch/>DEEP MEDIA LIBRARY · FIRST-PARTY VISUALS</span><h2 id='deep-media-title'>ספריית המדיה<br/><em>העמוקה.</em></h2></div><div><p>המקורות נשארים מקוריים, אבל התצוגה אינה תלויה יותר בצילום־מסך חיצוני שנשבר. וידאו מקבל פריים אמין; כתבות ופוסטים מקבלים פוסטר ראיה מקומי; ואיגור נשאר החוט האנושי לאורך כל הספרייה.</p><small><ShieldCheck/>Drive, Notion ו־Gmail משמשים לבקרת הקשר בלבד. האתר הציבורי מציג מקור ציבורי, מעמד ראיה ופוסטר עריכתי מסומן.</small></div></header><div className='deep-media-ledger' aria-label='נתוני ספריית המדיה'><article><strong>{deepMedia.length}</strong><span>רשומות מדיה ערוכות</span></article><article><strong>{sources}</strong><span>גופים ומשטחי מקור</span></article><article><strong>0</strong><span>צילומי־מסך אוטומטיים תלויים</span></article><article><strong>8</strong><span>שכבות תוכן</span></article><article><strong>IGOR</strong><span>החוט האנושי הקבוע</span></article></div><div className='deep-source-provenance'><span>FIRST-PARTY VISUALS</span><span>AUDITED PUBLIC INDEX</span><span>OWNER EXPORTS</span><span>MEDIA OS</span><span>RELEASE HISTORY</span></div><div className='deep-media-controls'><label><Search/><input value={query} onChange={event=>{setQuery(event.target.value);setExpanded(false)}} placeholder='חיפוש: StartOn, אבהות, נובה, מוזיקה…' aria-label='חיפוש בספריית המדיה'/>{query&&<button type='button' onClick={()=>setQuery('')} aria-label='ניקוי החיפוש'><X/></button>}</label><nav aria-label='סינון ספריית המדיה'>{deepMediaCategories.map(value=><button type='button' key={value} className={category===value?'active':''} onClick={()=>chooseCategory(value)}>{value}</button>)}</nav></div><div ref={stageRef} className='deep-media-stage'><div className='deep-stage-visual'>{active.youtubeId&&playing?<iframe src={`https://www.youtube-nocookie.com/embed/${active.youtubeId}?autoplay=1&rel=0&modestbranding=1`} title={active.title} allow='autoplay; encrypted-media; picture-in-picture' allowFullScreen/>:<MediaEvidenceVisual kind={active.youtubeId?'video':'post'} mode='stage' source={active.source} title={active.title} year={active.year} metric={active.metric} status={active.status} image={active.youtubeId?active.image:undefined} tone={active.category} index={activeIndex} disclosure={active.youtubeId?'תמונת וידאו מקורית של YouTube':'כרטיס ראיה עריכתי · המקור המלא נפתח בקישור'}/>} {!playing&&active.youtubeId&&<button type='button' className='deep-play' onClick={()=>setPlaying(true)}><Play fill='currentColor'/>לצפייה כאן</button>}{playing&&<button type='button' className='deep-stop' onClick={()=>setPlaying(false)} aria-label='סגירת הנגן'><X/></button>}</div><article className='deep-stage-copy'><div className='deep-stage-meta'><span>{active.category}</span><span>{active.year}</span>{active.language&&<span>{active.language}</span>}</div><small>{active.source}</small><h3>{active.title}</h3><p>{active.summary}</p>{active.metric&&<div className='deep-stage-metric'><b>{active.metric}</b><span>מדד נקודתי · אינו מחובר לסכום חשיפה כולל</span></div>}<div className='deep-stage-status'><ShieldCheck/><span><small>מעמד הראיה</small><b>{active.status}</b></span></div><a href={active.url} target='_blank' rel='noreferrer'>לפתיחת המקור המלא <ExternalLink/></a></article><aside className='deep-igor-thread'><figure><img src='./resources/igor-hero.jpg' alt='איגור ופרצקי' loading='lazy'/><span>APPROVED PUBLIC PORTRAIT</span></figure><div className='deep-igor-thread-copy'><small>THE HUMAN THREAD</small><h4>IGOR VEPRETSKI</h4><blockquote>{lens[active.category]}</blockquote><div className='deep-igor-thread-years'><div><b>HUMAN</b><span>אדם וניסיון חיים</span></div><div><b>MISSION</b><span>StartOn והזדמנות</span></div><div><b>SYSTEM</b><span>7YA, מקור וראיה</span></div></div></div></aside></div><div className='deep-media-results'><header><div><span>{filtered.length} פריטים מוצגים</span><b>{category==='הכול'?'כל הספרייה':category}</b></div><p>בחרו כרטיס כדי לפתוח אותו בבמה. כרטיסי כתבה ופוסט נבנים באתר; וידאו נטען רק לאחר לחיצה.</p></header>{visible.length?<div className='deep-media-grid'>{visible.map((item,index)=><button type='button' key={item.id} className={`${active.id===item.id?'active ':''}deep-card deep-card-${index%7}`} onClick={()=>chooseItem(item)} aria-pressed={active.id===item.id}><div className='deep-card-visual'><MediaEvidenceVisual kind={item.youtubeId?'video':'post'} mode='card' source={item.source} title={item.title} year={item.year} metric={item.metric} status={item.status} image={item.youtubeId?item.image:undefined} tone={item.category} index={index} disclosure={item.youtubeId?'ORIGINAL VIDEO THUMBNAIL':'7YA EDITORIAL EVIDENCE POSTER'}/></div><div><small>{item.source} · {item.year}</small><strong>{item.title}</strong>{item.metric&&<em>{item.metric}</em>}<span>{item.category}<ArrowUpLeft/></span></div></button>)}</div>:<div className='deep-media-empty'><Search/><h3>לא נמצאו פריטים</h3><p>נסו ניסוח אחר או חזרו ל״הכול״.</p><button type='button' onClick={()=>chooseCategory('הכול')}>איפוס הספרייה</button></div>}{filtered.length>12&&<button type='button' className='deep-expand' onClick={()=>setExpanded(value=>!value)}>{expanded?<><ChevronUp/>לצמצם את הקיר</>:<><ChevronDown/>להציג עוד {filtered.length-12} פריטים</>}</button>}</div><footer className='deep-media-footer'><div><span>THE COMPLETE DESK</span><h3>המקור נשאר בחוץ.<br/>הסיפור מקבל בית אמין בפנים.</h3></div><a href='?page=media'>לכל הראיונות והפודקאסטים <ExternalLink/></a></footer></section>}