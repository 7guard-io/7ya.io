# Deprecated hosting configuration

GitHub Pages is the only canonical production target for `7ya.io`.

The files in this directory preserve historical Vercel and Netlify redirect configuration for audit and rollback analysis only. They are intentionally stored outside the repository root so external hosting providers do not auto-detect them as active production configuration.

Do not restore these files to the repository root without a reviewed architecture decision.
