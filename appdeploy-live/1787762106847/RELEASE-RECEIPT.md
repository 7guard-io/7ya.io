# 7YA production release receipt — media corpus cutover — 2026-08-26

## Production authority
- Provider: AppDeploy v2
- App ID: `697a008fddc309b142`
- Applied snapshot: `1787762106847`
- Canonical domain: `https://7ya.io/`
- GitHub canonical repository: `7guard-io/7ya.io`
- GitHub export branch: `fix/media-corpus-cutover-20260826`

## User-visible intent
Stop treating live social OAuth as a prerequisite for showing Igor Vepretski's already-known public media. Materialize public/third-party source objects from the unified public projection and rank high-signal media before low-signal profile surfaces.

## Runtime delta
- `LiveSocial` now fetches `/api/public-projection?limit=100&sort=newest` in parallel with `/api/social-feed`.
- Projection records are converted into source-linked cards for YouTube, Instagram, TikTok, Facebook and LinkedIn when the source URL identifies the platform.
- Projection items are merged before live social and static public-register fallback, with canonical URL de-duplication.
- High-signal ranking favors video, verified Canon, Live/Discovery source layers, major media/podcast publishers and YouTube video objects.
- The homepage media shelf renders up to 24 ranked objects instead of the previous 18.
- The bundled public-register fallback remains active through `mergePublicRegisterIntoCanon(canonicalCorpusSeed)` even when live social credentials are absent.
- The public library remains open-ended and defaults to `Impact → first`; backend `projectionImpactScore` weights Canon/Live/Discovery state, source-local metrics, broadcast/press/podcast/repost signals, media type, relationships and visual availability without inventing cross-platform reach.

## Acceptance gates
- Test 3 explicitly requires the homepage media shelf to consume the public projection and stay populated without Instagram/Facebook/TikTok live OAuth.
- Test 7 requires public-register media to remain materialized under missing live credentials.
- Test 8 requires impact-first ordering and prohibits synthetic cross-platform reach.

## Release gate evidence
- AppDeploy terminal state: `ready`.
- Fresh AppDeploy QA after snapshot `1787762106847`: 0 frontend errors, 0 backend errors, 0 network errors reported.
- Desktop and mobile QA screenshots were generated by AppDeploy for QA snapshot `1787762155341`.
- Current chat runtime could not independently download the S3 screenshot pixels; this receipt therefore records deployment QA plus source-level verification, not an independent pixel audit.
- Source verification confirms `public-projection?limit=100&sort=newest`, `projectionItems`, `mergePublicRegisterIntoCanon` and `.slice(0,24)` in the applied `LiveSocial.tsx`.

## Live-provider boundary
The archive no longer depends on live OAuth. Live provider access is still additive:
- YouTube: public live feed.
- Instagram: access-token gated.
- TikTok: OAuth gated.
- Facebook: Page ID + Page access token gated.
- LinkedIn: member-post read remains restricted by platform approval.

## GitLab / Gmail findings
- GitLab mirror project `7ya-io/7yaio-Igor-Vepretski` has no active pipeline and is not the production authority for this cutover.
- Gmail alerts confirm the GitHub `Publish 7YA Static Production` and `Digital Museum Collector` workflows have been failing on current main; AppDeploy remains the active publication path while the GitHub source snapshot is reconciled.

## Rollback
Exact pre-functional-cutover AppDeploy snapshot: `1787762024475`.
