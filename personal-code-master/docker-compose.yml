services:
  code-master:
    build: .
    env_file:
      - .env
    environment:
      GEMINI_API_KEY: ${GEMINI_API_KEY}
      GEMINI_MODEL: ${GEMINI_MODEL:-gemini-3.5-flash}
    init: true
    read_only: true
    security_opt:
      - no-new-privileges:true
