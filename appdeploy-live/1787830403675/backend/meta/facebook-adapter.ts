import {SocialIngestRecord,validateSocialIngestRecord} from '../../shared/social-ingest.js';
import {MetaResolvedPage} from './capabilities.js';
import {MetaConfig,metaFetchJson} from './client.js';
export type MetaBatch={records:SocialIngestRecord[];nextCursor:string|null;fetchedAt:string};export type MetaNormalizeContext={apiVersion:string;accountObjectId:string;fetchedAt:string};export type MetaPageFetchContext={config:MetaConfig;page:MetaResolvedPage;cursor?:string|null;fetchedAt?:string};
type GraphBatch={data?:unknown[];paging?:{cursors?:{after?:string}}};const objectOf=(value:unknown):Record<string,unknown>=>value&&typeof value==='object'&&!Array.isArray(value)?value as Record<string,unknown>:{};const httpsValue=(value:unknown)=>{const text=String(value||'').trim();return /^https:\/\//i.test(text)?text:undefined};
const attachmentLooksVideo=(raw:Record<string,unknown>)=>{const attachments=objectOf(raw.attachments);const data=Array.isArray(attachments.data)?attachments.data:[];return data.some(value=>{const item=objectOf(value);return /video/i.test(String(item.media_type||''))||/video/i.test(String(item.type||''))})};
export function normalizeFacebookPost(value:unknown,ctx:MetaNormalizeContext):SocialIngestRecord|null{const raw=objectOf(value);const id=String(raw.id||'').trim();const canonicalUrl=httpsValue(raw.permalink_url);const publishedAt=String(raw.created_time||'').trim();if(!id||!canonicalUrl||!publishedAt)return null;const thumbnailUrl=httpsValue(raw.full_picture);return validateSocialIngestRecord({id:`meta:facebook:${id}`,provider:'meta',platform:'Facebook',providerObjectId:id,accountObjectId:ctx.accountObjectId,canonicalUrl,publishedAt,text:String(raw.message||'').slice(0,5000),mediaType:attachmentLooksVideo(raw)?'video':thumbnailUrl?'image':'post',...(thumbnailUrl?{thumbnailUrl}:{}),provenance:{source:'owner-authorized-api',fetchedAt:ctx.fetchedAt,apiVersion:ctx.apiVersion},metrics:[]})}
export async function fetchFacebookPageBatch(ctx:MetaPageFetchContext):Promise<MetaBatch>{if(!ctx.page.allowed||!ctx.config.allowedPageIds.has(ctx.page.pageId))throw new Error('Facebook Page is not allowlisted');const fetchedAt=ctx.fetchedAt||new Date().toISOString();const params:Record<string,string>={fields:'id,message,created_time,permalink_url,full_picture,attachments{media_type,type,url,media,target}',limit:'50'};if(ctx.cursor)params.after=ctx.cursor;const payload=await metaFetchJson<GraphBatch>(ctx.config,`${ctx.page.pageId}/posts`,params,ctx.page.pageAccessToken);const records=(Array.isArray(payload.data)?payload.data:[]).flatMap(value=>{const record=normalizeFacebookPost(value,{apiVersion:ctx.config.apiVersion,accountObjectId:ctx.page.pageId,fetchedAt});return record?[record]:[]});const nextCursor=String(payload.paging?.cursors?.after||'').trim()||null;return{records,nextCursor,fetchedAt}}
