# Cloudflare Pages full-build trigger — 2026-09-18

This commit intentionally triggers the first full governed Cloudflare Pages build after switching the project build command to `npm run build:cloudflare` and output directory to `dist`.

No DNS or custom-domain change is part of this trigger.
