name: Quarantined — legacy AppDeploy DNS preflight

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  quarantine:
    runs-on: ubuntu-latest
    steps:
      - name: Explain quarantine
        run: |
          echo 'QUARANTINED_WORKFLOW'
          echo 'The original DNS cutover path is historical. Current AppDeploy custom-domain state must be inspected through the active provider control plane before any future DNS work.'
