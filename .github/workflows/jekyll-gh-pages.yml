name: Quarantined — legacy Jekyll Pages deployment

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  quarantine:
    runs-on: ubuntu-latest
    steps:
      - name: Explain quarantine
        run: |
          echo 'QUARANTINED_WORKFLOW'
          echo '7ya.io production is AppDeploy. This historical Jekyll workflow is retained only for provenance and performs no deployment.'
