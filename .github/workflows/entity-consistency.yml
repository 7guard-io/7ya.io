name: Quarantined — legacy static entity consistency

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  quarantine:
    runs-on: ubuntu-latest
    steps:
      - name: Explain quarantine
        run: |
          echo 'QUARANTINED_WORKFLOW'
          echo 'This workflow targets the legacy static GitHub tree. Entity validation must be reconciled against the current AppDeploy runtime before automation is re-enabled.'
