(()=>{const root=document.createElement('div');root.className='ai-shell';root.innerHTML=`<button class="ai-orb" type="button" aria-label="פתחו את 7YA AI"><span>7</span><b>AI</b></button><section class="ai-panel" aria-hidden="true"><header><div><small>IGOR × AI</small><strong>7YA AI</strong><span>Human-first public guide</span></div><button class="ai-close" type="button" aria-label="סגירה">×</button></header><div class="ai-log" role="log" aria-live="polite"><div class="ai-msg bot">שלום. אני מדריך 7YA. שאלו אותי על איגור, StartOn, ראיות או איך משתמשים ב־AI כדי לשנות מציאות.</div></div><form><input name="message" maxlength="1600" autocomplete="off" placeholder="מה תרצו להבין?" aria-label="הודעה"><button type="submit">שליחה</button></form><footer>תשובות AI עשויות לטעות. טענות ציבוריות נבדקות מול Evidence Wall.</footer></section>`;document.body.append(root);const panel=root.querySelector('.ai-panel'),orb=root.querySelector('.ai-orb'),close=root.querySelector('.ai-close'),form=root.querySelector('form'),input=root.querySelector('input'),log=root.querySelector('.ai-log');function setOpen(v){panel.classList.toggle('open',v);panel.setAttribute('aria-hidden',String(!v));if(v)setTimeout(()=>input.focus(),80)}orb.onclick=()=>setOpen(!panel.classList.contains('open'));close.onclick=()=>setOpen(false);document.querySelectorAll('[data-open-ai]').forEach(b=>b.addEventListener('click',()=>setOpen(true)));function msg(text,type='bot'){const d=document.createElement('div');d.className='ai-msg '+type;d.textContent=text;log.append(d);log.scrollTop=log.scrollHeight;return d}form.addEventListener('submit',async e=>{e.preventDefault();const text=input.value.trim();if(!text)return;msg(text,'user');input.value='';input.disabled=true;const wait=msg('חושב…','bot waiting');try{const r=await fetch('/api/guide',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message:text,path:document.body.dataset.route||location.pathname})});const data=await r.json();wait.remove();msg(data.answer||'לא הצלחתי לענות כרגע.','bot');if(Array.isArray(data.links)){const row=document.createElement('div');row.className='ai-links';data.links.slice(0,3).forEach(x=>{const a=document.createElement('a');a.href=x.href;a.textContent=x.label;row.append(a)});log.append(row);log.scrollTop=log.scrollHeight}}catch{wait.textContent='החיבור לא זמין כרגע. אפשר להמשיך דרך מפת האתר.'}finally{input.disabled=false;input.focus()}})})();
