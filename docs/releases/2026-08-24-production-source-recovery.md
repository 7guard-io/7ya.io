# 7YA Production Source Recovery — Release Receipt

Date: 2026-08-24 (Asia/Jerusalem)
Status: PRODUCTION TRUTH REPAIR DEPLOYED · SOURCE PARITY STILL IN PROGRESS

## Purpose

This release removes three concrete production-truth defects without pretending that GitHub `main` is already deployment-identical:

1. restore the missing native `/journey/` critical route;
2. replace contradictory global production release identifiers with one release id;
3. correct repository governance so AppDeploy immutable snapshots, not historical Vercel state or rolling `vNN` labels, define current runtime provenance.

## Immutable AppDeploy provenance

- App id: `697a008fddc309b142`
- Frozen pre-repair recovery baseline: `1787521286005`
- Baseline time: `2026-08-24 00:41:26 Asia/Jerusalem`
- Test-contract snapshot: `1787521792664`
- Post-repair production snapshot: `1787521959471`
- Post-repair time: `2026-08-24 00:52:39 Asia/Jerusalem`
- AppDeploy display label at post-repair verification: `v98` — informational only; the label is rolling and is not durable provenance.
- Global production release: `7ya-production-truth-20260824-1`

## Production changes

### Test contract first

`tests/tests.txt` received Test 8 before route implementation:

`Keep the critical Journey route crawlable and native`

It requires:

- `/journey/` native crawlable HTML;
- canonical `https://7ya.io/journey/`;
- index/follow;
- no meta-refresh;
- a direct entry into the autobiographical journey;
- one global release id across the active release surfaces.

The frozen pre-repair snapshot had no `public/journey/`, so the new requirement was structurally RED before implementation. AppDeploy currently reports `e2e_tests=null`, therefore this receipt does **not** claim that an executable E2E runner produced a failing RED run or a passing GREEN run.

### GREEN repair

The final production repair changed:

- `public/journey/index.html` — new native Journey route;
- `src/App.tsx` — global frontend release id;
- `backend/index.ts` — global backend release id;
- `index.html` — global homepage release/build metadata;
- `public/release.json` — global release/build marker, Journey trust route and truthful E2E availability statement.

The global release id is now:

`7ya-production-truth-20260824-1`

Source verification on snapshot `1787521959471` found that id in:

- `src/App.tsx`;
- `backend/index.ts`;
- `index.html`;
- `public/journey/index.html`;
- `public/release.json`.

Historical component/subsystem release identifiers are intentionally not rewritten when they describe bounded artifacts rather than the global production build.

## Journey route verification

Snapshot `1787521959471` contains `public/journey/index.html` with:

- `robots=index,follow,max-image-preview:large`;
- canonical `https://7ya.io/journey/`;
- hreflang HE / EN / RU / x-default;
- no meta-refresh;
- a primary action into `../?lang=he#cinema-open`;
- a direct action into `../?lang=he#hundred-moments`;
- links to Timeline and Evidence.

## Fresh AppDeploy runtime QA

After the repair deploy, AppDeploy returned:

- deployment status: `ready`;
- frontend errors: `0`;
- network errors: `0`;
- backend errors: `0`;
- fresh desktop capture: generated;
- fresh mobile capture: generated;
- executable E2E status: not reported (`e2e_tests=null`).

The QA screenshot artifacts were generated by AppDeploy. Their direct S3 artifact path was not available for pixel-level inspection from the current execution environment, so this receipt records **capture generated**, not a fabricated visual-inspection PASS.

## Custom domains

Provider verification after the repair showed:

- `7ya.io`: `active`;
- `www.7ya.io`: `active`.

No DNS or nameserver change was made in this slice.

## GitHub recovery state

Recovery branch:

`recovery/appdeploy-1787521286005`

The branch now contains:

- corrected production/source governance;
- corrected 100 Moments snapshot provenance;
- recovery design and implementation plan aligned to the actual runtime architecture;
- frozen pre-repair recovery manifest;
- representative exact pre-repair runtime files;
- post-repair immutable README;
- exact deployed Journey route;
- post-repair release-delta manifest.

## Remaining parity debt

This receipt does **not** claim full source recovery.

Still pending:

- complete AppDeploy text-source export into immutable GitHub snapshot paths;
- complete binary/public-resource parity inventory;
- comparison of the recovered AppDeploy application tree against the older GitHub root application;
- selection of the canonical GitHub application path;
- executable `npm run release:gate` in a trusted environment;
- provider-reported E2E execution when available;
- pixel-level inspection of the final desktop/mobile QA captures;
- GitHub-driven deployment cutover only after the above gates pass.

Until then:

- AppDeploy immutable snapshot is runtime truth;
- GitHub is governance/review/recovery truth;
- `main` is **not** deployment-identical.

## Deferred integrations

Intentionally not started in this slice:

- Supabase migration;
- Windsor.ai ingestion;
- vidIQ ingestion;
- broad visual redesign.

Those should follow source-truth stabilization, not compete with it.

## Rollback

- Frozen recovery baseline: `1787521286005`.
- Immediate pre-final-repair provider snapshot: `1787521880042`.

Rollback must use immutable AppDeploy snapshot ids, not rolling `vNN` labels.

## Merge policy

A recovery PR may be opened for review. It must remain unmerged until the remaining parity/review gates are explicitly satisfied.