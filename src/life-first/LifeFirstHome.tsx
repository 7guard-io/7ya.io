import {useEffect} from 'react';
import AutobiographicalCinema from './AutobiographicalCinema';
export default function LifeFirstHome(){useEffect(()=>{let timers:number[]=[];const jump=()=>{const id=decodeURIComponent(window.location.hash.slice(1));if(!id)return;timers.forEach(timer=>window.clearTimeout(timer));timers=[80,500].map(delay=>window.setTimeout(()=>{const target=document.getElementById(id);if(target)window.scrollTo({top:Math.max(0,target.getBoundingClientRect().top+window.scrollY-68),behavior:'auto'})},delay))};jump();window.addEventListener('hashchange',jump);return()=>{timers.forEach(timer=>window.clearTimeout(timer));window.removeEventListener('hashchange',jump)}},[]);return <main className='life-first-home autobiographical-cinema-edition' data-edition='autobiographical-cinema-20260823'><AutobiographicalCinema/></main>}
