# Tests

## Test 1 - THE ECHO story selector changes the visible propagation chain [sanity]
Viewport: desktop
Covers: THE ECHO identity, three story selectors, active-story state, origin/distribution/response chain, evidence CTA
Description: Verifies the primary new interaction works without requiring a long tour through unrelated homepage sections.

Steps:
1. Open ?lang=he and locate THE ECHO / ההדהוד
2. Verify three story selector buttons are visible and select כשהגננת מתקשרת
3. Verify the active story now shows ORIGIN, DISTRIBUTION and RESPONSE nodes with evidence-state labels and at least one פתיחת הראיה link

Expected: THE ECHO is visible; exactly three curated story selectors are available; selecting כשהגננת מתקשרת changes the active story without a page reload and exposes a source-backed propagation chain with evidence states and evidence CTA.

## Test 2 - THE ECHO mobile chain stays inside the viewport and uses a designed source fallback
Viewport: mobile
Covers: responsive THE ECHO layout, mobile story switching, SOURCE PLATE fallback, horizontal-overflow guardrail
Description: Verifies the new experience remains usable on a narrow viewport and missing imagery never becomes an empty media hole.

Steps:
1. Open ?lang=he and locate THE ECHO
2. Select כשהגננת מתקשרת and verify its source area displays SOURCE PLATE / LINKED EVIDENCE
3. Verify the story selector, source plate and propagation nodes remain inside the viewport with no horizontal scrolling

Expected: The mobile layout stacks vertically; the image-less story shows a labeled source plate; source CTAs and evidence labels remain readable; page horizontal overflow is 0.

## Test 3 - Evidence states and dated metrics remain separate instead of becoming a vanity total
Viewport: desktop
Covers: verified/recovery evidence states, fatherhood source metrics, no-double-counting guardrail, no-fabricated-quote guardrail
Description: Verifies THE ECHO communicates evidence strength and native metrics without inventing a universal reach number or quotation.

Steps:
1. Open ?lang=he, locate THE ECHO and select אבא ששם
2. Verify the chain visibly includes a לשחזור / RECOVERY origin and a מאומת / VERIFIED redistribution or response node
3. Verify the source-backed values 4.1K reactions, 148 comments and 26 shares are visible and the section also states NO DOUBLE COUNTING · NO FABRICATED QUOTES

Expected: Evidence states remain visibly distinct; the known native metrics are shown as source snapshots; no combined total reach number and no quoted public response text are introduced.

## Test 4 - THE ECHO identity and core promise are localized in English and Russian
Viewport: desktop
Covers: HE/EN/RU localization, localized THE ECHO headline, stable story selector across locales
Description: Verifies the new impact experience is not Hebrew-only.

Steps:
1. Open ?lang=en and verify THE ECHO plus the headline What happens after I press Publish. and three story selectors
2. Open ?lang=ru and verify ЭХО / THE ECHO plus the Russian localized headline and three story selectors

Expected: English and Russian render localized THE ECHO copy while preserving the same three-story interaction and evidence structure.

## Test 5 - Visual QA reports zero release-blocking layout issues for THE ECHO
Viewport: mobile
Covers: ?visualQA=1 diagnostics, THE ECHO responsive layout, EMPTY MEDIA PANELS, HORIZONTAL OVERFLOW, FIXED OVERLAP, BROKEN IMAGES
Description: Verifies the client-side visual inspector sees no release-blocking media or layout regression after THE ECHO.

Steps:
1. Open ?lang=he&visualQA=1 and wait for the Visual QA audit to settle
2. Inspect THE ECHO and refresh the audit once
3. Verify EMPTY MEDIA PANELS, HORIZONTAL OVERFLOW, FIXED OVERLAP and BROKEN IMAGES each report 0 issues

Expected: THE ECHO is readable and contained on mobile; the Visual QA release-blocking media/layout gates listed above remain at 0 after refresh.
