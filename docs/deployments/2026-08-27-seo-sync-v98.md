# 7YA SEO + Media/Data Sync Cutover — 2026-08-27

## Production snapshot
- AppDeploy app: `697a008fddc309b142`
- Applied snapshot: `1787859400536`
- Production release marker: `7ya-production-acceptance-20260827-seo-sync-v2`
- Frontend release marker: `7ya-seo-sync-20260827-v9`
- Custom domains: `7ya.io` and `www.7ya.io` active

## Changes deployed
1. Replaced indexable redirect shells for `/media/`, `/library/`, `/research/`, `/music/`, `/speaker/`, `/blog/`, `/create/`, `/evidence/`, `/search/`, and `/museum/` with static-first crawlable HTML that hydrates into the live React application.
2. Removed redundant Hebrew `?lang=he` canonical URLs and normalized legacy `?page=` entry points to clean routes.
3. Rebuilt `sitemap.xml` around canonical public routes and removed admin/integrity surfaces from the sitemap.
4. Added a public JSON Feed at `/api/feed.json`, generated from the unified Public Projection (Canon + Discovery + live social/Meta projection).
5. Added feed discovery to root HTML and `llms.txt`.
6. Kept existing source ingestion and hourly jobs intact: `agent-mesh-hourly` and `meta-sync-hourly`.
7. Fixed four pre-existing Vite CSS public-asset paths blocking validation (`7ya-launch.webp`, `7ya-research.webp`).

## Verification evidence
- AppDeploy deployment status: READY.
- QA after deploy: 0 frontend errors, 0 backend errors, 0 network errors.
- `agent-mesh-hourly`: enabled, last status success, failure_count 0 at release check.
- `meta-sync-hourly`: enabled, last status success, failure_count 0 at release check.
- AppDeploy source grep across the ten SEO routes: no `meta refresh` and no `location.replace()` remain.
- Custom-domain control plane: both `7ya.io` and `www.7ya.io` active on AppDeploy v2.

## CI constraint
The exact local command `npm run ci:local` could not be executed in this ChatGPT runtime because outbound DNS to GitHub is unavailable. GitHub Actions in this repository is also documented as blocked before runner startup. The executable release gate used for this cutover was AppDeploy validation/build/runtime QA, and the first candidate correctly failed before deployment on invalid CSS asset references; those references were fixed before the successful release.

## Source-of-truth safety note
GitHub `main` at parent `0419e0b9119f4116e21861e283efa4fe75604e06` is materially behind the live AppDeploy snapshot; for example, it does not contain `src/documentary-home/`, which the production `src/App.tsx` imports. Therefore this release intentionally does **not** overwrite runtime files in `main` with a partial AppDeploy export: doing so would create a broken and potentially rollback-prone repository. Until a complete AppDeploy source export is committed atomically, the tested production source of truth for this release is AppDeploy snapshot `1787859400536`.

## Rollback reference
Previous applied snapshot: `1787840200798`.

## Policy
Do not deploy GitHub `main` over production until a complete source reconciliation/export has been performed and the canonical release gate passes.