# Efficient-build rationale

The Connections Hub deliberately avoids a new backend, CMS, database, OAuth broker or hosting migration. It is a static projection of a small safe registry, so it can ship independently and later consume verified runtime capability endpoints without being rewritten.
