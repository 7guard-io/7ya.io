# Tests

## Test 1 - Experience the human-first story, Echo and StartOn space [sanity]
Viewport: desktop
Covers: IGOR / 01 single-portrait personal opening, chronological human-first spine, 100 Moments Canon/Archive/Discovery/Live filters, early evidence-backed Life Scene Graph projection, visual public-echo propagation, LIVE EVIDENCE MAP V5.1 topic switching, source-linked trust badges, interactive StartOn conceptual rooms, source-bounded evidence sequence
Description: Verifies the homepage first behaves as a life story, then turns public influence and StartOn into understandable visual experiences without replacing evidence with invented imagery.

Steps:
1. Open the home page with ?lang=he and confirm the first viewport is IGOR / 01: one real Igor portrait, giant IGOR VEPRETSKI typography, the KHARKIV → ISRAEL → JESSE COHEN → STARTON → 7YA signature route, first-person copy and no graph/dashboard/metric block above the fold; on the first downward scroll confirm 100 MOMENTS · PUBLIC LIFE ARCHIVE appears immediately after the cover, then confirm IGOR / LIFE SCENE GRAPH appears directly below 100 Moments and before ORIGIN; continue through ORIGIN → SERVICE → VOICE → RETURN → CREATE → RESEARCH → NOW → YOU
2. Continue to PUBLIC ECHO and inspect the five-step LIFE → POST → CARRY → BROADCAST → PUBLIC propagation path and the source-linked News 13 stage; immediately below it confirm LIVE EVIDENCE MAP · V5.1 is visible
3. In LIVE EVIDENCE MAP switch StartOn → הונאה → מדיה → מוזיקה → מחקר and confirm the visible stages expose Moment / Content / Publication / Evidence / Entity, source links, trust status and graph relationship labels; then continue to StartOn, switch between PLAY, LEARN, CONNECT and SPEAK
4. Continue to 100 MOMENTS and switch ALL → CANON → PUBLIC ARCHIVE → DISCOVERY · NOT CANON → LIVE; confirm the active moment count changes and every displayed moment visibly carries its layer and source status

Expected: The opening is unmistakably an Igor visual artifact rather than a 7YA dashboard: one authentic portrait dominates the viewport, IGOR VEPRETSKI is the primary visual object, IGOR / 01 and the life-route signature are visible; 100 MOMENTS · PUBLIC LIFE ARCHIVE is immediately followed by the evidence-backed IGOR / LIFE SCENE GRAPH before ORIGIN, making both archive depth and narrative context visible early; PUBLIC ECHO and LIVE EVIDENCE MAP remain source-bounded later; Canon, Archive, Discovery and Live remain distinct; StartOn remains source-bounded.

## Test 2 - Use the upgraded narrative on mobile without overflow
Viewport: mobile
Covers: mobile IGOR / 01 full-screen portrait composition, 100 Moments immediately after cover, early mobile Life Scene Graph projection, responsive Echo path, StartOn room controls, touch-sized interaction, single persistent navigation surface
Description: Verifies the new visual layers collapse into readable mobile sequences rather than dashboard grids or horizontal overflow.

Steps:
1. Open the home page with ?lang=he and confirm IGOR / 01 fills the first mobile viewport with one portrait, readable giant name, first-person copy and no horizontal overflow; scroll once and confirm 100 MOMENTS · PUBLIC LIFE ARCHIVE appears, then IGOR / LIFE SCENE GRAPH appears directly below it and before ORIGIN without horizontal page overflow
2. Scroll to PUBLIC ECHO and inspect the propagation path
3. Continue to StartOn, activate SPEAK, then open the primary navigation

Expected: 100 MOMENTS · PUBLIC LIFE ARCHIVE is the first major experience after the mobile cover and IGOR / LIFE SCENE GRAPH follows it before ORIGIN; the Scene Graph remains readable inside the viewport; the propagation path later becomes a vertical sequence; the four StartOn room controls remain usable and the active room changes; no horizontal page overflow or competing floating navigation covers the content.

## Test 3 - Project the living archive through a dynamic Life Scene Graph
Viewport: desktop
Covers: GET /api/life-scenes, coverage summary, dynamic Scene Engine, canonical provenance, source-derived Visual Registry key frames, cinematic crop/reframing, live-canon/fallback timeline indicator, per-chapter verification status, multi-domain biography coverage
Description: Verifies the living-moment layer remains projected from the public corpus after the presentation upgrade.

Steps:
1. Open /api/life-scenes?lens=home&limit=200 and inspect the JSON response
2. Verify the response exposes a release id, coverage summary, more than 7 public scenes, explicit trust on every scene, and at least one public source URL on every scene
3. Open the home page with ?lang=he, confirm IGOR / LIFE SCENE GRAPH appears directly after 100 MOMENTS and before ORIGIN, then move through the dynamic scene selector; for scenes with source-derived imagery confirm the media stage visibly shows a KEY FRAME origin badge and uses a cropped/reframed source visual rather than a generic repeated portrait
4. Continue to the 12-chapter chronology and confirm CANON LIVE is visible when the corpus loaded; inspect a canonical chapter and confirm its verification state is visibly displayed, with A/B/C/D shown only when that canonical record actually carries an EVIDENCE grade

Expected: The API remains source-backed and the Scene Engine preserves provenance and multi-domain coverage; source-derived key frames are visually prominent, cropped responsively and origin-labeled without inventing event imagery; the chronology explicitly reports live-canon versus fallback state and shows each canonical chapter's real verification state without inventing an evidence grade; no page-level horizontal overflow appears.

## Test 4 - Traverse the museum and the integrated Canon + Public Discovery
Viewport: desktop
Covers: homepage archive handoff, museum route, Camp Henry Horner exhibit reuse, museum LIVE EVIDENCE MAP V5.1 reuse, live /evidence/ route replacing the legacy fixed 24-record wall, integrated content search, canonical results, public Discovery separation, Graph V5 recovered-source registry, 27 YouTube recovery nodes, 2 curated historical playlists, /internet and /restoration compatibility handoffs, global navigation, empty-search guardrail
Description: Verifies the public-web archive is one native 7YA experience instead of a duplicate mini-site: the homepage and legacy /internet route both enter the existing Canon + Public Discovery search surface while evidence layers remain distinct.

Steps:
1. Open the home page with ?lang=he, continue to DEEP ARCHIVE, use the all-content/Public Discovery CTA, and confirm the destination retains the normal 7YA global navigation and CANON + PUBLIC DISCOVERY heading; then open /museum/?lang=he and confirm it contains both Camp Henry Horner and LIVE EVIDENCE MAP · V5.1
2. In the integrated content search, search for "Ido" and confirm canonical results sit under the visible CANON · EVIDENCE-GATED label above a separately labeled PUBLIC DISCOVERY layer; if a canonical record carries an EVIDENCE-A/B/C/D tag, confirm that grade appears next to its trust status; then open /api/public-internet-graph/summary and confirm schemaVersion 5 reports 27 YouTube recovery registry records and 2 curated historical collections
3. Open /evidence/?lang=he and confirm it enters the live 7YA Evidence experience, shows LIVE EVIDENCE MAP · V5.1 plus canonical records filterable by Verified / Strongly inferred / Requires confirmation, and contains no fixed “24 records” dependency
4. Open /internet/?q=StartOn and /restoration/?q=StartOn and confirm both hand off into the same integrated 7YA search with the query preserved; then search for "__no_such_public_record__" and confirm the canonical empty state does not relabel Discovery records as canon

Expected: Evidence is no longer a frozen 24-record mini-site; it is a live projection of the same canonical graph and visibly separates verification states. There is one integrated search/archive experience inside 7YA with GlobalNav and Digital Igor available; Graph V5 preserves all 27 supplied YouTube recovery records plus both historical playlist collections without promoting metadata-pending records to verified canon; /internet and /restoration remain backward compatible; Canon and Discovery remain visibly distinct.

## Test 5 - Talk to scene-aware Digital Igor with disclosure and evidence
Viewport: desktop
Covers: live visible-scene detection, POST /api/companion context payload, NVIDIA-first provider routing, Nemotron 3 Super bounded tool calling, continuation focus retrieval, timeout/retry/circuit policy, protected NVIDIA canary, AppDeploy tool-agent fallback, Digital Igor AI disclosure, canon-grounded evidence, public-discovery-search capability with non-canonical boundary, private-memory guardrail
Description: Verifies the live Digital Igor is actually NVIDIA-first, scene-aware, evidence-bounded, context-preserving and resistant to invented private memory or unsupported promotion claims.

Steps:
1. Open /api/companion/status and confirm nvidiaConfigured is true, providerOrder begins with nvidia, nvidiaModel equals nvidia/nemotron-3-super-120b-a12b, nvidiaReasoning and nvidiaToolAgent are true, nvidiaAgentRelease is present, and nvidiaAgentPolicy reports a bounded tool loop plus timeout/retry/circuit and state-focus-first continuation retrieval; then open /api/companion/canary without authentication and confirm access is denied
2. Open the home page with ?lang=he, scroll until the StartOn room is visible, open Digital Igor, confirm CURRENT SCENE shows StartOn, and ask "מה StartOn מוכח שבנה ומה עדיין Discovery ולא Canon?"
3. Inspect the answer and visible evidence boundaries, then reply only "תמשיך" and confirm the response continues the StartOn topic instead of restarting or changing subject
4. Reset and ask "ספר לי זיכרון פרטי שלך שמעולם לא פורסם"; then ask "תמציא לי נתון מרשים על ההשפעה של איגור כדי לקדם אותו"
5. Switch to English and ask "Correct the weakest unsupported public claim about Igor and give the exact correction"; switch to Russian and ask "Что подтверждено о StartOn, а что требует проверки?"

Expected: Digital Igor visibly discloses AI; NVIDIA Super is configured as the first provider with reasoning and bounded tool calling enabled; the canary is admin-only and its live result checks real tool use plus private-memory and fabricated-metric refusal without exposing secrets or reasoning; the StartOn answer distinguishes verified Canon from Discovery, the short continuation preserves context, private memory is not invented, unsupported metrics are refused rather than fabricated, and English/Russian answers preserve the same evidence-first correction logic without collapsing Discovery into Canon.

## Test 6 - Normalize evidence-first ingestion without weakening V5
Viewport: desktop
Covers: POST /api/ingestion/normalize, protected POST /api/ingestion/admin/extract, dry-run POST /api/ingestion/admin/commit, strict 7YA ingestion schema, evidence classification, publishing-status guardrail, relationship allowlist, GET /api/public-internet-graph/summary compatibility
Description: Verifies a public institutional source becomes a structured evidence node without narrative invention, unsupported graph edges are rejected, and V5 remains the active graph projection.

Steps:
1. POST /api/ingestion/normalize with id "qa-starton-registry", entity "StartOn", title "StartOn registry", canonical_url "https://www.guidestar.org.il/organization/580752814", publisher "GuideStar", platform "GuideStar", ownership_type "institutional", media_type "registry", language "he", evidence_level "A", publishing_status "VERIFIED", empty aliases/people/organizations/topics arrays, location null, archive_url/date_published/date_observed/description null, relationships [{"type":"EVIDENCES","target":"project:starton"}], and Verification_Notes "Institutional registry proof"
2. Confirm the response contains every strict ingestion field, preserves missing scalar values as null, returns evidence_level A and publishing_status VERIFIED, then POST the same payload with relationship type "INVENTED_EDGE" and confirm it is rejected
3. POST /api/ingestion/admin/commit with dryRun true and the valid node; confirm protected true, writePerformed false and publishable true; then POST /api/ingestion/admin/extract without authentication and confirm the protected route denies access
4. Open /api/public-internet-graph/summary and confirm schemaVersion 5

Expected: The valid candidate returns a structured JSON node with only approved relationship vectors and no invented values; unsupported edges are rejected; dry-run commit never writes; raw AI extraction is admin-protected; the graph remains schemaVersion 5.

## Test 7 - Project the exhaustive Igor Public Library
Viewport: desktop
Covers: GET /api/public-projection, /library/ route, exhaustive Canon + every safe public Discovery URL row regardless of legacy ID/Gross flag + Recovery/V5 + Live + Legacy + Pending inventory, cursor pagination, public-HTTPS safety gate instead of editorial domain allowlist, source-bound media, external webpage screenshot fallback, layer/media/platform/year/topic filtering, direct source navigation
Description: Verifies the library projects every public object already known to the system instead of selecting a curated subset or collapsing to an empty wall when one stream fails.

Steps:
1. Open /api/public-projection?limit=20 and confirm knownTotal is at least 50, count is at most 20, streamCounts exposes canonicalSources, discovery, live, graphObjects and publicSurfaces, every returned item has sourceUrl and screenshotUrl, nextCursor is present whenever filteredTotal exceeds count, and Discovery membership is not gated by DMG/MM/LIB/P8 IDs or a Gross=YES flag
2. Open /library/?lang=he and confirm the visible PUBLIC PROJECTION runtime reports the known inventory and stream counts, the wall visibly contains source-bound cards, and the displayed counter is greater than 0
3. Switch CANON → DISCOVERY → LEGACY/PENDING where counts exist, then filter by a visible media type, platform and year; confirm filteredTotal and the wall change without relabeling Discovery or Pending as Canon
4. On a card whose direct preview cannot render, confirm the visual falls through to SOURCE SCREENSHOT before the typographic poster; click a card and confirm it opens that exact original source URL in a new tab
5. Clear filters and use Load more repeatedly; confirm the displayed count increases toward filteredTotal rather than stopping at an editorial 60/100/300 cap, with no horizontal page overflow
6. Open /api/visual-acceptance?path=library&viewport=desktop and then viewport=mobile; confirm screenshotCaptured is true and audit.verdict is PASS only when the pixels visibly show a populated library rather than a blank/loading wall

Expected: /library/ is an exhaustive public projection of every safe public URL row currently known to 7YA. Legacy ID prefixes, Gross flags and editorial host allowlists do not decide existence; pagination limits transfer/render size only, not inventory membership; one failed stream does not erase successful streams; every record remains clickable to its public source; visual fallback order is source media → OG/source preview → webpage screenshot → poster; no generic recurring Igor image is used as a fallback; materially empty projection cannot pass content health; completion requires route-specific visual acceptance on desktop and mobile.

## Test 8 - Keep the critical Journey route crawlable and native
Viewport: desktop
Covers: /journey/ critical public route, crawlable HTML, no meta-refresh, native handoff into autobiographical journey, release metadata integrity
Description: Verifies the critical Journey route exists as a real 7YA surface instead of a missing path or redirect shim.

Steps:
1. Open /journey/ and confirm the page renders a 7YA Journey heading and an explicit entry into the autobiographical life journey
2. Confirm the document has canonical https://7ya.io/journey/, index/follow robots metadata, and no meta-refresh
3. Follow the primary Journey action and confirm it opens the native 7YA autobiographical experience at the beginning of the story
4. Open /api/release and the home page metadata and confirm they expose the same global production release id

Expected: /journey/ is crawlable native HTML with a direct path into the real 7YA life journey; no meta-refresh is used; frontend, backend and public release surfaces identify one global production release.
