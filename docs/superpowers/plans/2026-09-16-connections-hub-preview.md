# Connections Hub preview gate

The feature is ready for review only after the branch checks pass and a preview can be produced from the canonical repository. Production alias/DNS must not change as part of preview. Vercel browser authentication is currently a separate blocker to reconnecting its Git integration; it is not a blocker to completing the canonical branch.