name: Actions Smoke

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ok:
    name: Minimal runner validation
    runs-on: ubuntu-latest
    steps:
      - name: Runner can execute shell
        run: echo "ACTIONS_SMOKE_PASS"
