# AppDeploy Runtime Reconciliation Checkpoint — 1788005385311

Date: 2026-08-29
App: `697a008fddc309b142`
AppDeploy label: `v98`
Snapshot: `1788005385311`
Canonical domain: `https://7ya.io/`
Build marker: `7ya-cinematic-os-20260828-v1`
Source alignment: `APPDEPLOY_LIVE_SNAPSHOT_PENDING_GITHUB_EXPORT`

## Verified runtime state

At inspection time, AppDeploy reported the deployment as `ready` with no current frontend or backend errors. The AppDeploy custom-domain control plane reported both `7ya.io` and `www.7ya.io` as active.

The runtime `public/release.json` identifies:

- service: `7ya-frontend`;
- provider: `appdeploy`;
- environment: `production`;
- experience: `CINEMATIC_PERSONAL_OS_V1`;
- languages: Hebrew, English and Russian;
- source alignment: `APPDEPLOY_LIVE_SNAPSHOT_PENDING_GITHUB_EXPORT`;
- rollback snapshot: `1787938474434`.

Automated E2E status was not available in the inspected AppDeploy status response, so no E2E PASS is claimed.

## Runtime tree enumeration

A paginated `src_glob` enumeration of snapshot `1788005385311` was completed across the available AppDeploy source tree. The live runtime is not the legacy static/Jupyter application found at the GitHub root.

Observed runtime areas include:

### Frontend

- Vite + React 19 application (`src/main.tsx`, `src/App.tsx`, `vite.config.ts`);
- cinematic/personal homepage systems under `src/life-first/`;
- documentary, influence, media, museum, music, research, speaker, StartOn, archive and public-library components;
- responsive/mobile and RTL presentation layers;
- route-specific static projections under `public/`.

### Backend

- `backend/index.ts`;
- evidence ingestion and corpus storage;
- growth, realtime and life-scene services;
- Meta Instagram/Facebook adapters, OAuth bridge, synchronization and capability/store modules.

### Shared data and evidence contracts

- canonical corpus/entities;
- content graph;
- evidence-first ingestion;
- impact universe/broadcast;
- life scenes;
- media impact;
- public internet graph;
- public register canon;
- recovered publications;
- social ingestion.

### Public routes/data

The runtime tree includes projections for the core 7YA experience, including `/igor-vepretski/`, `/journey/`, `/starton/`, `/influence/`, `/evidence/`, `/speaker/`, `/talk/`, `/contact/`, plus media, museum, music, research, library, search, privacy and related routes.

It also contains machine-readable evidence/entity data, schemas, `robots.txt`, `llms.txt`, sitemap, manifest and release metadata.

### Operations

- `cron.json` with active AppDeploy-side scheduled work;
- tests under `tests/`;
- AppDeploy authentication metadata;
- runtime package configuration using Vite/React rather than the legacy root package/application shape.

## Important package divergence

The live AppDeploy `package.json` is a Vite/React application (`vite-react-app`) with React 19 and Vite 6. The GitHub root `package.json` is a different legacy governance/static-site package with many historical checks and backend dependencies.

This confirms that replacing AppDeploy from the GitHub root tree before reconciliation would be unsafe.

## What this checkpoint does not claim

This checkpoint records a complete path enumeration and selected source/config reads. It does **not** claim that every file's full content has been exported to GitHub.

`full_runtime_tree_export_complete` therefore remains `false`.

## Next source-control gate

Before GitHub `main` can become deployable production source again:

1. Export the complete content of snapshot `1788005385311` into an isolated reconciliation tree/branch.
2. Compare it with the GitHub application tree and existing immutable `appdeploy-live/` records.
3. Resolve routes, backend behavior, public data, evidence policy, privacy boundaries and provider-specific configuration.
4. Run `npm run release:gate` or an equivalent reconciled gate on the resulting canonical tree.
5. Record an explicit cutover/rollback receipt.

## Legacy GenAI curriculum

The inherited `generative-ai-for-beginners` lesson directories and translations are confirmed repository legacy, not the architecture of the inspected live AppDeploy runtime. They are **not removed in this checkpoint** because deletion belongs after the complete runtime export/reference comparison. This prevents repository cleanup from being mistaken for source reconciliation.
